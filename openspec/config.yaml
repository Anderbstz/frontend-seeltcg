# OpenSpec Configuration — frontend-seeltcg
# Auto-generated by sdd-init @ 2026-06-08

context: |
  Next.js 16.2.7 frontend for a Pokémon TCG marketplace (PikaCards).
  App Router, Tailwind CSS v4, TypeScript 5, ESLint 9.
  Stripe integration for payments.
  No test runner configured — frontend-only project.

strict_tdd: false
# No test runner detected (frontend-only project). Strict TDD disabled.

testing:
  runner: null
  layers:
    unit: { available: false, tool: null }
    integration: { available: false, tool: null }
    e2e: { available: false, tool: null }
  coverage: { available: false, command: null }
  quality:
    linter: { available: true, command: "npm run lint" }
    type_checker: { available: true, command: "npx tsc --noEmit" }
    formatter: { available: false, command: null }

phases:
  proposal: {}
  spec: {}
  design: {}
  tasks: {}
  apply: {}
  verify:
    # Verify runs manually — no CI or test runner to automate.
    manual_only: true
  archive: {}
